connector.name=iceberg
iceberg.catalog.type=rest
iceberg.rest-catalog.uri=http://iceberg-rest:8181
iceberg.file-format=PARQUET
iceberg.rest-catalog.warehouse=s3://lakehouse/
iceberg.rest-catalog.vended-credentials-enabled=false

fs.native-s3.enabled=true
s3.endpoint=http://minio:9000
s3.aws-access-key=minio
s3.aws-secret-key=minio12345
s3.path-style-access=true
s3.region=us-east-1





#########################
# connector.name=iceberg
# iceberg.catalog.type=rest
# iceberg.rest-catalog.uri=http://nessie:19120/iceberg
# iceberg.file-format=PARQUET
# iceberg.rest-catalog.warehouse=s3://lakehouse/

# fs.native-s3.enabled=true

# s3.endpoint=http://minio:9000
# s3.path-style-access=true
# s3.aws-access-key=minio
# s3.aws-secret-key=minio12345
# s3.region=us-east-1

# iceberg.rest-catalog.security=STATIC
# iceberg.rest-catalog.s3.aws-access-key=minio
# iceberg.rest-catalog.s3.aws-secret-key=minio12345

# iceberg.rest-catalog.warehouse=s3://lakehouse/


# connector.name=iceberg
# iceberg.catalog.type=rest
# iceberg.rest-catalog.uri=http://nessie:19120/iceberg
# iceberg.file-format=PARQUET
# iceberg.rest-catalog.warehouse=s3://lakehouse/
# iceberg.rest-catalog.vended-credentials-enabled=false

# fs.native-s3.enabled=true
# s3.endpoint=http://minio:9000
# s3.aws-access-key=minio
# s3.aws-secret-key=minio12345
# s3.path-style-access=true
# s3.region=us-east-1


